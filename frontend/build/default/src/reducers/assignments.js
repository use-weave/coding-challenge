import
/* */
'../actions/assignments.js';
const INITIAL_STATE = {};

const assignments = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default assignments;